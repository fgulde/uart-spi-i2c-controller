{
    "version": "2.0.0",
    "inputs": [
        {
            "id": "moduleName",
            "type": "promptString",
            "description": "Module name without extension, e.g. uart_tx, uart_rx, spi_master, i2c_master (expects rtl/<name>.sv + tb/tb_<name>.sv)",
            "default": "uart_tx"
        }
    ],
    "tasks": [
        {
            "label": "Simulate: UART TX",
            "type": "shell",
            "command": "just sim uart_tx",
            "options": { "cwd": "${workspaceFolder}" },
            "group": { "kind": "test", "isDefault": true },
            "problemMatcher": [],
            "presentation": { "clear": true, "panel": "shared" }
        },
        {
            "label": "Simulate: Module (pick)",
            "type": "shell",
            "command": "just sim ${input:moduleName}",
            "options": { "cwd": "${workspaceFolder}" },
            "group": "test",
            "problemMatcher": [],
            "presentation": { "clear": true, "panel": "shared" }
        },
        {
            "label": "Waveform: Module (pick)",
            "type": "shell",
            "command": "just wave ${input:moduleName}",
            "options": { "cwd": "${workspaceFolder}" },
            "problemMatcher": []
        },
        {
            "label": "Synth-Check: Module (pick, no board)",
            "type": "shell",
            "command": "just synth ${input:moduleName}",
            "options": { "cwd": "${workspaceFolder}" },
            "problemMatcher": [],
            "presentation": { "clear": true, "panel": "shared" }
        },
        {
            "label": "Lint: Module (pick)",
            "type": "shell",
            "command": "just lint ${input:moduleName}",
            "options": { "cwd": "${workspaceFolder}" },
            "problemMatcher": [],
            "presentation": { "clear": true, "panel": "shared" }
        },
        {
            "label": "CI: all modules (lint + sim + synth)",
            "type": "shell",
            "command": "just ci",
            "options": { "cwd": "${workspaceFolder}" },
            "problemMatcher": [],
            "presentation": { "clear": true, "panel": "shared" }
        }
    ]
}
